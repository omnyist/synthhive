services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: synthhive
      POSTGRES_USER: synthhive
      POSTGRES_PASSWORD: synthhive
    volumes:
      - postgres_data:/var/lib/postgresql/data

  server:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      - db
    environment:
      - DATABASE_URL=postgresql://synthhive:synthhive@synthcore-postgres-1:5432/synthhive
      - REDIS_URL=redis://synthcore-redis-1:6379/1
    volumes:
      - .:/app
    ports:
      - "7177:7177"
    networks:
      - default
      - synth
    command: >
      sh -c "uv run python manage.py migrate &&
             uv run python manage.py seed &&
             uv run python manage.py collectstatic --noinput &&
             uv run daphne -b 0.0.0.0 -p 7177 synthhive.asgi:application"

  bot:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    depends_on:
      - db
      - server
    environment:
      - DATABASE_URL=postgresql://synthhive:synthhive@synthcore-postgres-1:5432/synthhive
      - REDIS_URL=redis://synthcore-redis-1:6379/1
    volumes:
      - .:/app
    networks:
      - default
      - synth
    command: uv run python manage.py runbot

networks:
  default:
  synth:
    external: true

volumes:
  postgres_data:
